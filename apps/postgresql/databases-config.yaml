apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-databases
  namespace: postgres
data:
  databases: | # db -> user
    ptcgl-dev ptcgl
    aviator-db aviator
    coder-db coder
    audio-streamer audio-api
