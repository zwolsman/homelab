apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-databases
  namespace: postgres
data:
  databases: | # app/db user
    ptcgl-dev ptcgl
    aviator-db aviator
    coder-db coder
    audio-api audio-streamer
